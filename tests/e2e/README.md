# End-to-end tests (WS5)

Drive the server over the **stdio MCP transport** as a client would (primary user journey:
open session → import → analyze → decompile/list/search → close), asserting envelopes and bounds
end to end. Synthetic fixtures only.
