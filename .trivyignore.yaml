# Trivy vulnerability waivers — TIME-BOXED, justified, owned (workflow-cve-management /
# workflow-vuln-mgmt). Each entry AUTO-EXPIRES on `expired_at`; after that date Trivy re-includes
# the CVE and the image-scan gate re-blocks — there are NO permanent ignores.
#
# NO ACTIVE WAIVERS (2026-06-10). The six Debian-base CVEs that previously lived here (perl-base +
# ncurses, from the `python:3.12-slim` layer) were retired by the distroless migration (PLAN §9):
# BOTH images now run on Chainguard/Wolfi bases (server: cgr.dev/chainguard/python; worker:
# cgr.dev/chainguard/wolfi-base + apk python-3.12 + openjdk-21). Wolfi ships no perl and its
# packages are CVE-tracked/patched — Trivy HIGH,CRITICAL OS scan = 0 for both images. There is
# nothing left to waive.
#
# This file is retained (wired via `trivyignores:` in .github/workflows/worker-image.yml and
# documented in infra/trivy.yaml) as the home for any FUTURE time-boxed, justified waiver — add a
# `{id, statement (VEX reachability + compensating controls), expired_at}` entry below if one is
# ever genuinely needed; never a permanent ignore.
#
# Owner: 0xb007ab1e.
vulnerabilities: []
