"""Bundled, versioned data artifacts shipped inside the ``vivarium`` package.

Currently the offline **family-match corpus** (:mod:`vivarium.data.family_corpus`, ADR-073 D3).
These are trusted, in-repo data (NOT binary-derived) — offline by construction, so consulting them
never crosses a network trust boundary (containment intact, ADR-001/ADR-005).
"""
